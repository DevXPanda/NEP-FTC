import type { NextConfig } from 'next';

// Next.js 15 configuration for nep-web (App Router). Placeholder.
const nextConfig: NextConfig = {
  reactStrictMode: true,
  output: 'standalone',
};

export default nextConfig;
