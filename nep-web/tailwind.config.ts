import type { Config } from 'tailwindcss';

// Tailwind CSS configuration for nep-web. Placeholder.
const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
