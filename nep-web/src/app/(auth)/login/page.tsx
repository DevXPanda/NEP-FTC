// Login page — collects credentials and exchanges them for JWT access/refresh tokens.
export default function LoginPage() {
  return (
    <section>
      <h1>Sign in to NEP</h1>
      {/* TODO: login form */}
    </section>
  );
}
