// Inventory module — list view (maps to the "inventory" ERP capability route).
export default function InventoryPage() {
  return (
    <section>
      <h1>Inventory</h1>
      {/* TODO: inventory content */}
    </section>
  );
}
