# nep-web — environment configuration (copy to .env.local and fill in real values)

# Base URL of the NEP API Gateway (exposed to the browser)
NEXT_PUBLIC_API_GATEWAY_URL=http://localhost:8080

# Secret used to sign/encrypt auth sessions
NEXTAUTH_SECRET=change-me
